fastapi
uvicorn[standard]
pydantic
sqlalchemy
passlib[bcrypt]
# Pinned: passlib 1.7.4 (its last release) probes an internal `bcrypt.__about__`
# attribute that bcrypt>=4.1 removed, breaking hashing with a spurious
# "password cannot be longer than 72 bytes" error. 4.0.1 is the newest
# release passlib's version-sniffing still works against.
bcrypt==4.0.1
pyjwt
python-multipart
